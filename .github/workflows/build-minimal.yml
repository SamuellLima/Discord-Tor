name: build-discord-tor

on:
  workflow_dispatch:
  push:
    paths:
      - 'golive-minimal/**'
      - '.github/workflows/build-minimal.yml'

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version: '1.26.5'
          cache-dependency-path: golive-minimal/go.mod
      - run: go test ./...
        working-directory: golive-minimal
      - name: Build Windows launchers
        working-directory: golive-minimal
        shell: pwsh
        run: |
          $env:CGO_ENABLED = '0'
          go build -trimpath -buildvcs=true -ldflags="-s -w" -o DiscordTor-debug.exe ./cmd/golive-tor
          go build -tags silent -trimpath -buildvcs=true -ldflags="-s -w -H=windowsgui" -o DiscordTor.exe ./cmd/golive-tor
          (Get-FileHash -Algorithm SHA256 .\DiscordTor-debug.exe).Hash.ToLower() | Set-Content -NoNewline DiscordTor-debug.exe.sha256
          (Get-FileHash -Algorithm SHA256 .\DiscordTor.exe).Hash.ToLower() | Set-Content -NoNewline DiscordTor.exe.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: DiscordTor-windows-amd64
          path: |
            golive-minimal/DiscordTor.exe
            golive-minimal/DiscordTor.exe.sha256
            golive-minimal/DiscordTor-debug.exe
            golive-minimal/DiscordTor-debug.exe.sha256
          if-no-files-found: error
